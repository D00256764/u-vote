# Legacy manual install — prefer overlays/staging/istio.yaml via Argo CD.
apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: uvote-gateway
  namespace: uvote-dev
spec:
  selector:
    istio: ingressgateway
  servers:
  - port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - "*"

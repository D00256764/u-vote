# Example only — apply with plat_scripts/setup_alertmanager_slack.sh instead of committing real URLs.
apiVersion: v1
kind: Secret
metadata:
  name: alertmanager-slack
type: Opaque
stringData:
  # File must contain only the webhook URL (no trailing newline required).
  slack_api_url: "https://hooks.slack.com/services/REPLACE/ME"

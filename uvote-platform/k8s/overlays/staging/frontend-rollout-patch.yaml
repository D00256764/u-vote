# Staging: skip Prometheus analysis gates (often fail with sparse traffic → Degraded rollout).
# Keeps Istio canary routing but promotes to 100% immediately.
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: frontend-service
spec:
  strategy:
    canary:
      steps:
        - setWeight: 100
